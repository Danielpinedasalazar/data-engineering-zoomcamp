# Module 1: Containerization & Infrastructure as Code

- [`docker_sql/`](docker_sql) — NY Taxi data pipeline containerized with Docker, loaded into Postgres, queried with SQL.
- [`terraform/`](terraform) — Same infrastructure (GCS bucket + BigQuery dataset) provisioned as code with Terraform on GCP.
